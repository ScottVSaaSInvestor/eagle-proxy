EAGLE VISION V6 - VERCEL DEPLOYMENT CHECKLIST
============================================

Current Status: Functions NOT deployed (Vercel project is empty)
Target: Deploy serverless function to handle API proxy

DEPLOYMENT STEPS:
-----------------

□ Step 1: Add files to GitHub repo
   - Navigate to: https://github.com/ScottVSaaSInvestor/eagle-proxy
   - Click "Add file" → "Upload files"
   - Upload ALL THREE files maintaining structure:
     • api/v1/messages.js
     • vercel.json
     • README.md
   
   OR use Git command line:
   ```
   cd /path/to/eagle-proxy
   mkdir -p api/v1
   # Copy the three files to correct locations
   git add .
   git commit -m "Deploy Anthropic API proxy serverless function"
   git push origin main
   ```

□ Step 2: Wait for Vercel auto-deployment
   - Watch Vercel dashboard: https://vercel.com/scottvsaasinvestors-projects/project-gfln8
   - Deployment should start automatically (~30 seconds after push)
   - Wait for "Ready" status (~60 seconds total)

□ Step 3: Verify deployment
   - Go to Vercel → project-gfln8 → Functions tab
   - You should see: "api/v1/messages.js" listed
   - Function should show recent invocations after testing

□ Step 4: Test Eagle Vision
   - Open: eagle-vision-v6.html in browser
   - Click "+ Add Company"
   - Enter API key and company name (e.g., "AxisCare")
   - Click "Start Pipeline"
   - Should see research modules running (not "Failed to fetch" error)

□ Step 5: Verify AxisCare scoring
   - Route: Should be "Play1"
   - SAI 1.0: Target 90-95/100
   - VAI 1.0: Target 95-100/100

EXPECTED RESULTS:
-----------------
✓ Vercel Functions tab shows: api/v1/messages.js
✓ Eagle Vision pipeline starts successfully
✓ No "Failed to fetch" or "Network error" messages
✓ Research modules complete
✓ Scoring completes with Play1 route
✓ AxisCare scores in expected ranges

FILES TO UPLOAD:
----------------
All files are in: /mnt/user-data/outputs/eagle-proxy-deployment/

Structure must be EXACTLY:
eagle-proxy/
├── api/
│   └── v1/
│       └── messages.js    ← Serverless function
├── vercel.json            ← Vercel config
└── README.md              ← Documentation

TROUBLESHOOTING:
----------------
If deployment fails:
1. Check GitHub repo structure matches exactly
2. Verify Vercel is still connected to GitHub repo
3. Check Vercel deployment logs for errors
4. Ensure vercel.json is valid JSON

If function deploys but returns errors:
1. Check Vercel Functions tab for error logs
2. Verify API key starts with "sk-ant-"
3. Test endpoint directly: POST to https://project-gfln8.vercel.app/v1/messages

PRODUCTION URL CONFIGURED:
--------------------------
HTML file already updated to use: https://project-gfln8.vercel.app/v1/messages
No HTML changes needed once function is deployed.
